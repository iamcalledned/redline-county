# REDLINE COUNTY implementation plan

1. A: preserve the tool pins, verify extensions, configure local editor tasks and build a separate smoke place. Establish a safe local Rojo connection where possible.
2. B: build a small original handling course, one shared arcade chassis with three tunings, keyboard/touch input, chase camera, server-owned spawn/reset lifecycle and instrumentation.
3. Gate: test acceleration, braking, slides, slopes, recovery, touch and two clients in Studio before expanding the map. A CLI build cannot verify physics.
4. C–D: add validated solo delivery/checkpoint progression, then police dispatch and interruptible capture; test abuse cases and duplicate payouts.
5. E–F: expand the proven course into the coastal county and add the Hot Car event, cosmetic progression, session-locked persistence, onboarding and polish.

No publishing, paid assets, account changes or live DataStore access are part of this setup. The development prototype uses no persistence.

## User-directed B+ expansion

The user accepted the driving/drift/open-driving checks and requested an endless Autobahn simulator with turbochargers. Implemented the highway/turbo slice before C–F; validate using AUTOBAHN.md next. Preserve the original cops-and-getaway roadmap while following this new direction.
